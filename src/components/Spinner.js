import React from 'react'

const Spinner = () => {
  return (
    <div style={{minHeight:'80vh', display:'flex',justifyContent:'center', alignItems:'center', width:'100%'}}>
        <div class="spinner"></div>
    </div>
  )
}

export default Spinner;